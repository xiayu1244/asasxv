# โปรแกรมคำนวณหาค่าเฉลี่ยของตัวเลขที่ผู้ใช้ป้อนเข้ามา

# รับตัวเลขจำนวนเต็มจากผู้ใช้
numbers = []
n = int(input("ป้อนจำนวนตัวเลข: "))

# รับตัวเลขแต่ละตัวและเก็บในรายการ numbers
for i in range(n):
    num = float(input("ป้อนตัวเลขที่ %d: " % (i+1)))
    numbers.append(num)

# คำนวณค่าเฉลี่ย
total = sum(numbers)
average = total / n

# แสดงผลลัพธ์
print("ค่าเฉลี่ยของตัวเลขคือ:", average)
